name: build releases

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64-unknown-linux-gnu
          - os: ubuntu-24.04-arm
            arch: aarch64-unknown-linux-gnu
          - os: macos-latest
            arch: x86_64-apple-darwin
          - os: macos-latest
            arch: aarch64-apple-darwin
    steps:
      - name: Get commit count
        run: echo "COMMIT_COUNT=$(git rev-list --count HEAD)" >> $GITHUB_ENV

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          target: ${{ matrix.arch }}
          toolchain: stable

      - name: Build Rust binary
        run: cargo build --release --target ${{ matrix.arch }}

      - name: Rename binary
        run: mv target/${{ matrix.arch }}/release/reposync target/${{ matrix.arch }}/release/reposync-${{ matrix.arch }}

      - name: Upload binary
        uses: softprops/action-gh-release@v2
        with:
          files: target/${{ matrix.arch }}/release/reposync-${{ matrix.arch }}
          tag_name: v$COMMIT_COUNT
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
